# FeatherFlap runtime configuration
# Values in this file override defaults defined in src/featherflap/config.py

# UPS telemetry addresses (Seengreat board responds at 0x43). Use JSON numbers (decimal).
FEATHERFLAP_UPTIME_I2C_ADDRESSES=[67]

# INA219 shunt resistor value (ohms) for current calculations
FEATHERFLAP_UPTIME_SHUNT_RESISTANCE_OHMS=0.01

# Nominal battery capacity (mAh) used by the learning estimator and scripts
FEATHERFLAP_BATTERY_CAPACITY_MAH=10000
